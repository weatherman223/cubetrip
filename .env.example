# WARNING: Set to "true" for local development to avoid hitting live Google Flights.
# Only set to "false" when you need real flight price data.
USE_MOCK_FLIGHTS=false

# Override SQLite database path (default: ./data/cache.db)
# DB_PATH=./data/cache.db
